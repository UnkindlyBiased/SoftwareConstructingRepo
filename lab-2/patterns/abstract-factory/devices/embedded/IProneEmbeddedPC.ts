import EmbeddedPC from "../../abstraction/EmbeddedPC";

export default class IProneEmbeddedPC implements EmbeddedPC {
    startEnterpriseApp(): void {
        console.log("KFC servance app is started")
    }
}